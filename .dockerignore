# Python
venv/
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Environment — never bake secrets into the image
.env

# Git + DVC metadata
.git/
.dvc/
*.dvc
dvc.lock

# Data — mounted as volumes, not baked into image
data/vectorstore/*
data/processed/*

# Dev files
test_setup.py
*.md
.gitignore
.dockerignore