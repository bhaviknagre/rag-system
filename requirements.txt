# ── API ──────────────────────────────────────
fastapi==0.139.0
uvicorn[standard]==0.32.1
gunicorn==23.0.0
pydantic==2.13.4
pydantic-settings==2.12.0
python-dotenv==1.2.2
requests==2.33.0

# ── Monitoring ───────────────────────────────
prometheus-fastapi-instrumentator==8.0.2
prometheus-client==0.21.1

# ── LangChain ────────────────────────────────
langchain==0.3.30
langchain-community==0.3.31
langchain-core==0.3.86
langchain-text-splitters==0.3.11
langchain-huggingface==0.1.2
langchain-chroma==0.1.4
langchain-pinecone==0.2.13
langchain-mongodb==0.3.0
langchain-experimental==0.3.4

# ── Embeddings ───────────────────────────────
sentence-transformers==3.3.1

# ── Vector DBs ───────────────────────────────
chromadb==0.5.23
pinecone==7.3.0
pymongo==4.10.1
certifi==2026.6.17

# ── Document loaders ─────────────────────────
pypdf==6.14.2
python-docx==1.1.2
unstructured==0.18.18

# ── Background jobs ──────────────────────────
celery==5.3.6
redis==5.0.1
flower==2.0.1

# ── Data versioning ──────────────────────────
dvc==3.50.1
pathspec==0.11.2

# ── Utilities ────────────────────────────────
tqdm==4.67.1
pyyaml==6.0.2
python-multipart==0.0.32

# ── Security-pinned transitive deps ──────────
# Pulled in indirectly with lax version caps from older sub-dependencies;
# pinned explicitly so the resolver can't silently settle on a vulnerable build.
aiohttp==3.14.1
starlette==1.3.1

# ── Docs ─────────────────────────────────────
mkdocs-material==9.7.6
mkdocs-minify-plugin==0.8.0
